# fallon-pdf-extract 处理流程

PDF → 结构化 Markdown 的提取流程，分输入、提取（5 阶段）、质量校验三层。本技能以**提取**为主，不做总结。

## 1 触发与输入

### 1.1 触发场景
- 用户提供 PDF 路径，要求转成 Markdown
- 提炼 PDF 的某部分 / 某几章（如"第 8–17 章"）
- 知识库 PDF 批量转 Markdown

### 1.2 输入参数
- `pdf`（必填）：PDF 路径
- `--chapters "8-17"`：从 TOC 自动定位章节起止页
- `--pages "222-393"`：直接指定 1-based 页码范围
- `--outdir`：输出目录（默认 PDF 同目录）
- `--title` / `--lang en|zh`：标题与语言

## 2 提取主流程（extract_pdf.py，5 阶段）

### 2.1 阶段一：范围定位
解析 `--chapters` 走 `get_toc()` + `parse_chapter_label()` 正则定位每章起始页；无 TOC 或 `--pages` 时直接映射页码；全文档为兜底。

### 2.2 阶段二：字号探测（全文档采样）
跨页均匀采样（约 30 页覆盖全文）→ 统计字号频率 → `body_size` 取字符量最大档；更大字号降序取前 4 档映射 h2–h5 的 `level_map`。自适应 A4/Letter。

### 2.3 阶段三：逐页提取
每页依次处理：

#### 2.3.1 图片提取
`get_image_info()` 取 xref + 位置 → 优先 `extract_image()` 存无损原图，失败回退 `get_pixmap(2x)` 渲染 → 存 `images/fig_p{页码}_{序号}.{ext}`；仅提取尺寸 >20px、非页眉页脚的图。

#### 2.3.2 表格提取
`find_tables()` 取表（过滤 Abstract/图形乱码伪表）→ `Table.to_markdown()`（支持合并单元格）；跨页表按页分别提取（PyMuPDF 为单页检测，无跨页自动拼接）。

#### 2.3.3 文本块清洗与定级
- 整块页眉/页脚先丢（须在脚注判定前）
- 清洗：ligature 规范化、分数保留原字符、`\x01/\x02`→`×`、软连字符删除
- 多栏检测：按行中心 x 聚类分栏 + 栏内 y 排序，纠正双栏交错
- 定级：`level_map` 命中→标题；`body<字号≤body+2.5` 且含人名→`*作者*`；正文块内识别 bullet→列表项，其余→段落
- 断词复原：行尾 `-`+下词小写→合并

#### 2.3.4 脚注识别
页底明显小字（`y>90%·页高` 且 `字号<body-0.5`）→ 标记 `> *注：...*`（实验特性）

### 2.4 阶段四：跨页合并与图注去重
- 跨页衔接：上段无句末标点 + 下段小写开头 → 合并（p→p、li→li、p/li 续接）
- 图注去重：正文流里与图注重复的段落移除，图注只跟图出现一次

### 2.5 阶段五：渲染输出与校验
- 图下方 `_find_caption()` 匹配 `^Figure/Fig./Map/Chart...` → `*图注*` 斜体，紧跟图不插空行
- 连续列表项间不插空行（修复列表断裂）
- 输出 `<PDF名>.md`，打印 `chars / h2 / h3 / tables / figures` 统计

## 3 质量校验（verify.py，可选）

转完 PDF 后可选运行，纯本地、零 LLM token。

### 3.1 召回率兜底
原文 `get_text()` 全量 vs MD 纯文字量比值，接近 1 说明无丢字（图片文字不计入，含图 PDF 偏低属正常）。

### 3.2 随机抽段本地 diff
按页分层抽样（层数=抽样段数，seed 可复现），每层随机抽一页，从该页原文随机抽段在 MD 全文滑窗搜最相似子串，报告相似度。

### 3.3 抽样规模规则
`n = clamp(round(√页数 / 1.5), 5, 15)`：小材料≈5，100 页≈7，大材料封顶 15；单段上限 800 字。低于阈值（默认 0.6）的片段列出原文与 MD 对照，供复查。

## 4 关键技术规则（摘要）
页眉页脚用相对边距（6%/94%·页高）；字号探测全文档采样；作者行判定前置；列表项连续不插空行；跨页合并覆盖三类衔接；图片存原图 + 图注进 MD。

## 5 已知边界
- 扫描版 PDF（无文字层）需 OCR，本技能不做
- 脚注 / 多栏三栏 / 中文"图X"为实验或未覆盖项
- 图片统一置于页末（非原文坐标）
- 原图存于输出目录根，可作二次素材（如小红书配图）
